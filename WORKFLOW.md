# 编译原理课程设计工作流程

## 版本控制

当前环境里的 `.git` 目录是只读空目录，不能作为普通 Git 仓库使用。因此本项目使用分离仓库目录：

```sh
./tools/gitw status
./tools/gitw add <file>
./tools/gitw commit -m "message"
./tools/gitw diff
```

Git 元数据保存在 `.vcs/git`，已在 `.gitignore` 中忽略。

## 目录约定

- `编译原理课程设计/`：原始课程资料，尽量不改动。
- `src/`：自己的编译器实现代码。
- `tests/`：从课程测试包中整理出来的测试输入、期望输出和测试脚本。
- `docs/`：从 PDF/PPT 中整理出的需求、文法、实验说明。
- `report/`：实践报告草稿和最终内容。
- `build/`：构建产物，只作为临时输出，不纳入版本控制。

## 推荐节奏

1. 每完成一个小功能就运行测试。
2. 测试通过后提交一次，提交信息写清楚实验阶段和功能点。
3. 需要大改前先提交当前可运行状态。
4. 原始资料不直接修改；需要摘录时放到 `docs/` 或 `report/`。

## 实验推进顺序

1. 整理 C-- 文法和测试规范。
2. 实践 1.1：Flex 词法分析。
3. 实践 1.2：Bison 语法分析。
4. 实践 2：语义分析。
5. 实践 3：中间代码生成。
6. 实践 4：MIPS 目标代码生成。
7. 实践 5：代码优化。
