int add(int x, int y) {
    return x + y;
}

int main() {
    int a, b;
    int arr[10];

    a = 1;
    b = add(a, 2);
    arr[0] = b;

    if (a < b && !(b == 0)) {
        a = arr[0] + b;
    } else {
        a = 0;
    }

    while (a > 0) {
        a = a - 1;
    }

    return a;
}
