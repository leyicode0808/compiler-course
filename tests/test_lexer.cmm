struct Point {
    int x;
    float y;
};

int main() {
    int a;
    float b;

    a = 10;
    b = 01.23E+2;

    // single-line comment
    if (a >= 10 && !(a != 0)) {
        return a;
    }

    while (a > 0) {
        a = a - 1;
    }

    return 0;
}
